<tr class="justify-content-between d-flex">
  <td><%= student.name %></td>
  <td>
    <%= link_to 'Edit', edit_student_path(student), class: 'btn btn-warning btn-sm' %>
    <%= link_to 'Delete', student_path(student), data: { turbo_method: :delete, confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %>
  </td>
</tr>
